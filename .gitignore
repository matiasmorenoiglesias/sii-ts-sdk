node_modules/
dist/
*.log
.DS_Store

# Certificados y CAF reales — nunca commitear
*.p12
*.pfx
*.pem
!fixtures/**/*.p12
!fixtures/**/*.pem
caf-real*.xml

# Carpeta para credenciales/archivos reales de prueba local, nunca se sube
.local/

# Variables de entorno locales (contraseñas, etc.), nunca se sube
.env
